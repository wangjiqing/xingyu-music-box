# Apple 客户端

Apple 客户端当前以 iOS 为主，使用 SwiftUI 和 Xcode 开发。

当前 iOS 工程位于 `apps/apple/ios/`，工程名、Target、Scheme、Bundle Identifier 和 Swift 源码模块名保持为现有命名。

后续计划在现有工程基础上扩展 iPadOS 与 macOS 体验。相关适配应优先复用现有业务能力，并保持与星语音库 OpenAPI 的联动，包括服务信息、曲目、歌词、封面和元数据等能力。

当前文档只描述本地客户端定位，不承诺 App Store 或 TestFlight 发布计划。
